/**
 * Marcador de versión de la app móvil, visible en Configuración.
 *
 * PARA QUÉ SIRVE: confirmar, desde la propia tablet, qué versión está corriendo.
 * Es también la PRUEBA de que la carga desde Vercel funciona: cambia este número,
 * haz push (Vercel despliega) y reabre la app. Si el número cambia SIN reinstalar
 * el APK → la app se está actualizando desde Vercel. ✅
 */
export const APP_VERSION = '1.0.2';
